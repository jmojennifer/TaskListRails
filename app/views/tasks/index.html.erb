<h1><%= @name %>'s Task List</h1>

<h2>Tasks</h2>

<% @all_tasks.each do |task| %>
    <h3><%=link_to(task[:name], task_path(task[:id])) %></h3>
    <br>
<% end %>

<h4><%=link_to("Create a task", tasks_new_path) %></h4>
